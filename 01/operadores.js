
const pi = 3.14;
let r = 35;

area = pi*(r**2);

console.log(`El area del circulo es ${area}`);
console.log("El area del circulo es", area);
console.log("El area del circulo es " + area);

let cuadrada = area**(1/2); // **eleva a la potencia /
let rcubica = area**(1/3);

console.log(`la raiz cuadrada del area es ${cuadrada} y la raiz cubica es ${rcubica}`);

let numeroCualquiera = 25;
console.log(numeroCualquiera % 2 == 0);//operador (resta) "%" devuelve el resto de la división entre dos operandos.